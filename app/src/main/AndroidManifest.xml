<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ks54">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ks54">
        <activity android:name=".LogoClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".College"/>
        <activity android:name=".Study"/>
        <activity android:name=".Razraby"/>
        <activity android:name=".Category"/>
        <activity android:name=".Podcategory"/>
        <activity android:name=".odnogrup"/>
        <activity android:name=".Vlados"/>
    </application>

</manifest>